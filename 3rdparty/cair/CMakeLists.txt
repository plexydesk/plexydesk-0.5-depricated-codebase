SET (seamsrc
    CAIR.cpp
    )

ADD_LIBRARY (seam SHARED ${seamsrc})

#SET_TARGET_PROPERTIES(seam PROPERTIES
#                      COMPILE_FLAGS ${CMAKE_SHARED_LIBRARY_CXX_FLAGS})


INSTALL(TARGETS seam DESTINATION ${CMAKE_INSTALL_LIBDIR})
